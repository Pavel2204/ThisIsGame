package com.chugunoff.GEngine.Utils;

import javax.swing.*;

public class IconInfo {
    public static void getSize(ImageIcon icon) {
        System.out.println(icon.getIconWidth() + "x" + icon.getIconHeight());
    }
}
